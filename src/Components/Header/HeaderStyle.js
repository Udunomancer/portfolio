import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
    typographyStyles: {
        flex: 1,
    },
    headerStyles: {
        marginBottom: "10px",
    },
}))

export default useStyles;