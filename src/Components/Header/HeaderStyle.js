import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
    typographyStyles: {
        flex: 1,
    },
    // headerStyles: {
    //     marginBottom: "0px",
    // },
}))

export default useStyles;